/**
 * Historical entry point: image work is handled by `optimize-assets.js`
 * (AVIF conversion + reference updates). Kept so `bun run optimize:all` keeps working.
 */
import './optimize-assets.js';
